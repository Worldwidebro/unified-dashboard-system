version: '3.8'

services:
  unified-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - billionaire-empire
    restart: unless-stopped

networks:
  billionaire-empire:
    driver: bridge
